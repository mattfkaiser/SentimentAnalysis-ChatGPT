The difference is that in those cases the AI was used to create meta data that didn't previously exist, however generative AI like ChatGPT uses and exploits data that existed previously and it creates an unlicensed derivative work of said data.